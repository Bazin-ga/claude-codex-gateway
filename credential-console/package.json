{
  "name": "@claude-codex-gateway/credential-console",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Multi-account credential control plane and per-device gateway for Codex and Claude Code.",
  "scripts": {
    "build:metrics-assets": "node scripts/build-metrics-assets.mjs",
    "check:metrics-assets": "node scripts/build-metrics-assets.mjs --check",
    "start": "node --no-warnings server.js",
    "test": "node --no-warnings --test --test-timeout=60000",
    "check": "node --no-warnings --check server.js && node --no-warnings --check cli.js && node --no-warnings --check web/metrics-echarts.entry.js && npm run check:metrics-assets"
  },
  "engines": {
    "node": ">=22.5.0"
  },
  "devDependencies": {
    "echarts": "6.1.0",
    "esbuild": "0.28.2"
  }
}
