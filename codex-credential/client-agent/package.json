{
  "name": "@claude-codex-gateway/codex-client-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Pulls a short-lived Codex credential from the dispenser and installs it into ~/.codex/auth.json.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "pull": "node pull.js",
    "pull:force": "node pull.js --force",
    "test": "node --test"
  }
}
