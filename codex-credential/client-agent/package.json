{
  "name": "@claude-codex-gateway/codex-client-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Pulls short-lived Codex credentials into isolated, atomically selectable client profiles.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "pull": "node pull.js",
    "pull:force": "node pull.js --force",
    "test": "node --test"
  }
}
